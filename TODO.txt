TODO list for function-builder

Refine query parameter for screen initialization strategy (onStartup vs onDemand)

Add query parameter for scene initialization strategy (onStartup vs onDemand)

Limit to 1 "parked" card in builder

Performance (responsiveness)

Performance (startup time)

Change requests

Mystery screen:
- pool of predefined function combinations
- radio buttons to select number of functions: single, double, triple,
- "generate" button to select random combination from pool
- assign function colors randomly
- cards: [-4,6], blank spot where 'x' would be
- eraser button
- Reset All
- "Show functions" button
- table drawer
- graph drawer

Code review
