TODO list for function-builder

---

Mystery screen mock up: 1 single function, 1 double function, 1 triple function, random generators for single/double/triple

Equation (see equations-formats.md), slope-intercept first

Table
- auto-scroll the table whenever a user action modifies the table
- rows in table are unique (no duplicates)
- when a card is removed from input carousel, call XYTableNode.addEntry(x)
- when a card is added to the input carousel, call XYTableNode.removeEntry(x)
- when a card is added to the output carousel, call XYTableNode.setYVisible(x,true)
- when a card is removed from the output carousel, call XYTableNode.setYVisible(x,false)

Reset All

---

Vertical seam visible at left or right edge of BuilderNode clipArea for some window sizes