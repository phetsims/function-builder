TODO list for function-builder

Make cards stop at 'see inside' windows when animating

Update card when location changes or Builder.functionChangedEmitter.emit

Handle non-invertible functions

Disable erase button if there's nothing in the output carousel

Should 'see inside' windows be added dynamically, and only where there's a function?

---

Need a way to pre-populate output carousel with 1 card of each type for testing convenience.

Move function to adjacent empty slot instead of returning to carousel.

Reset All doesn't work for things that the user is dragging

Vertical seam visible at left or right edge of clipArea for BuilderNode

Usability:
- How to keep functions that are mutable from popping out of the carousel and builder?